aoflagger.sumthreshold{masked}(
          converted_data,
          {converted_copy}
          sumthr_level,
          sumthr_level * transient_threshold_factor,
          true,
          true
        )